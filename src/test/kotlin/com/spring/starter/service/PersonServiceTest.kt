package com.spring.starter.service

import com.spring.starter.repository.PersonRepository
import com.spring.starter.service.impl.PersonServiceImpl
import com.spring.starter.validation.ValidationUtils
import org.mockito.Mockito.mock
import javax.validation.Validator

class PersonServiceTest {
    //val personRepository: PersonRepository = mock()
    //val personService: PersonService = PersonServiceImpl(personRepository, ValidationUtils(validator = Validator))
}